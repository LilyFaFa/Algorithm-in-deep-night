#最短路径问题
问题：n个城市，城市之间存在单向的行班，中间可以经停最多k站。
1、可以使用搜索算法，dfs，bfs但是要注意剪枝，否则时间会超时
算法复杂度是O(n的k次方)
剪枝方法：如果当前的费用大于已经找到路径的最大值，那么就停止搜索
2、可以使用动态规划算法，使用bellman-ford，动态规划算法
动态规划方程：dp[k][v] = min{dp[k-1][u]+cost[u][v], dp[k-1][v]}

