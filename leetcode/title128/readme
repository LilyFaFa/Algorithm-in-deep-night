最长连续子串
问题：
	给定一组数据，找出元素可以组成的最长的连续子串
	复杂度要求为O(n)，所以只能使用hashTable
	栗子：[1,6,32,8,2,5,3]结果就是[1,2,3]
难点在于：难度要在O(n)，问题重点需要有一个结题思路
解决方式：
    对每一个值进行判断，例如4，看3和5是否在hashtable中，在的话就可以和3一组，不在的话就只能单独成组
	判断在不在就使用hashtable