问题：
	给一组面额，给定一个数值
	求最少需要多少给定面额的钞票可以正好凑齐给定的数值，每种面额都可以重复使用
解决：使用动态规划，需要找到子问题和问题转移方程，背包问题
dp[i][j]：使用前i中硬币，达到数值j的最少硬币数量
dp[i][j]=min{dp[i-1][j], dp[j][i-count[j]]+1]}
注意点：因为记录的是最小值，所以在对二维切片进行初始化的时候需要都初始化成一个最大值