Easy
二叉排序树剪裁问题，二叉树一般使用递归调用
问题：
    给定一棵二叉树和一个取值范围，令二叉树节点的值在这个范围之内
	不符合规定的节点要被删除
解决：
    1、如果根节点小于范围最小值，节点以及左子树全部剪掉，新的根节点为右子树的剪裁结果
	2、如果根节点大于范围最小值，节点以及右子树全部剪掉，新的根节点为左子树剪裁结果
	3、如果根节点在范围内，那么对左子树和右子树进行剪裁
