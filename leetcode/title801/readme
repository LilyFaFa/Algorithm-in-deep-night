#问题：
给定两个相同长度的数组，A和B。其中对于A[i]和B[i]，可以进行交换。求问：经过几次交换之后会使得
两个数列都是单调递增的。
解答：
1、可以使用dfs来搜索，也就是对于每一个点都只有两个选择。可以实现一下这个函数。可以通过下面的剪枝
对于元素A[i]和B[i]而言，如果他们都比A[i-1]和B[i-1]大，那么可以选择交换或者是不交换。
如果B[i-1]>A[i]>A[i-1]并且A[i-1]>B[i]>B[i-1]那么为了满足升序，就必须要交换，还有一种
一定不能交换的类似情况
2、
A[i][0]: 表示前i个元素，并且第i位位置不交换的最少的交换次数。
A[i][1]: 表示前i个元素，并且第i位位置交换的最少的交换次数。
这样使用动态规划，计算子问题，通过小问题构建大问题的解，来记录中间的子问题。
这个与普通递归的区别就是没有一步都是要记录两个状态而不是一个状态