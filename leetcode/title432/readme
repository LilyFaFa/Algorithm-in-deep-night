设计一个数据结构，是一道设计类型的题目
Inc(Key):添加一个新的key，值为1。如果已经有这个key就加一。
Dec(Key):将key的值减少1。如果key的值为1，那么就删掉这个key。
GetMaxKey():返回值最大的key
GetMinKey():返回值最小的key
所有的操作是O(1)
解题：
1、根据key查询value，O(1):hashTable。
2、返回最大值最小值的复杂度为O(1)可以使用链表来串联（Vector，List）
3、Inc／Dec：使用List
设计：两种连，一条是频率连，从高到低排序；对于每一个频率连有具有该频率的键值对串成的连；来解决增删问题
还要使用一个hash表，来确定某一个元素是否存在的问题