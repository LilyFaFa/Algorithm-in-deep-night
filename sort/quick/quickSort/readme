#快速排序
使用到了递归，因为左右两侧都可以以相同的方法进行处理，直至小序列长度为1
在实际的应用中，快速排序是性能很好的排序算法.
算法步骤：设立左右两个指针，5，3，8，6，4。右侧指针先扫描，扫描到比5小的元素4就停止；左侧指针
再扫描，扫描到比5大的元素8停止，然后交换8和4继续扫描，然后两个指针到同一个元素，交换4和5，生成
一个序列，序列的左侧是小于5，右侧是大于5。
然后左右组序列依照这个方法进行递归排序，直至整个队列有序


平均的时间复杂度为：O(n*lgn)